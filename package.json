{
  "name": "openeditors-root",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter openeditor-text build",
    "test": "pnpm --filter openeditor-text test",
    "test:e2e": "pnpm --filter playground test:e2e",
    "test:all": "pnpm test && pnpm test:packages && pnpm test:e2e",
    "lint": "eslint packages/core/src/ packages/core/tests/ packages/react/src/ packages/react/tests/ packages/vue/src/ packages/vue/tests/ packages/cli/ packages/entitlements/ premium/ apps/playground/src/",
    "test:packages": "pnpm -r --filter '!openeditor-text' --filter '!openeditors-root' --filter '!playground' test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "1.60.0",
    "eslint": "^10.5.0",
    "globals": "^17.7.0",
    "rollup": "4.62.0",
    "vite": "5.4.21",
    "vitest": "1.6.1"
  }
}
